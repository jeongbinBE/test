<div class="container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			
			<h2>계정 설정 변경</h2>
			<%= form_for(:user, url: user_path, method: :patch) do |f| %>
				<%= render 'fields', f: f %>
				<%= f.submit "변경사항 저장", class: "btn btn-primary btn-orange" %>
			<% end %>

		</div>
	</div>
</div>

